# Cloudflare Pages + Functions. See README for KV setup.
# Must match the Pages project name in Cloudflare Dashboard (Workers & Pages → your project).
# If your project is named differently, change this to that exact name.
name = "dontgetgot"
pages_build_output_dir = "./dist"
compatibility_date = "2024-01-01"

[[kv_namespaces]]
binding = "ROOMS_KV"
id = "73b570bfa83e48b0847d5e62586b9002"
