/** Number of personal missions assigned to each player (excluding the common mission). */
export const PERSONAL_MISSION_COUNT = 5;

/** Total missions per player: 1 common + PERSONAL_MISSION_COUNT. */
export const TOTAL_MISSIONS_PER_PLAYER = 6;
